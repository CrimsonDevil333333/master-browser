{
  "name": "master-browser-root",
  "version": "0.1.7",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/cli": "^1.6.3"
  },
  "overrides": {
    "react-json-view": {
      "react": "19.2.3"
    }
  }
}
